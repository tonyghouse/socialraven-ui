export default function InfoCard() {
  return (
    <div className="my-6 p-4 bg-gradient-to-r from-cyan-50 to-purple-50 rounded-lg border border-cyan-200/50">
      <p className="text-sm text-foreground">
        Connect multiple accounts to schedule posts across all platforms at once.
        Your accounts are securely encrypted and stored.
      </p>
    </div>
  );
}
